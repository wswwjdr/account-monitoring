# 系统功能

本系统是在本机使用的账号管家，用来集中管理账号。当前版本面向 Outlook 个人邮箱，并查看收件箱邮件。登录不靠单独输入密码联网读信，而是导入一套完整凭证：邮箱地址、密码、应用标识（Client ID）、刷新令牌。密码主要用于本地备案和列表展示；真正读取邮件使用刷新令牌换取访问权限。

系统仅在本机使用，账号凭证保存在本地。不要放到公网，也不要把本地数据当作可公开资料。

---

## 账号管理

在「账号管理」中查看已导入的全部账号。每一行代表一个账号版本，展示邮箱、密码、所属分组、导入时间、令牌状态，并可进入查看邮件或删除该条记录。

支持按邮箱地址搜索，也支持按分组筛选。未选择分组时显示全部账号。

### 导入账号

通过粘贴文本一次性导入一个或多个账号。每一行必须是一个完整账号，包含四个字段：邮箱地址、密码、Client ID、刷新令牌。字段之间用制表符或四个短横线分隔。缺字段、格式不对时，整批导入失败，不会写入。

导入前可选择目标分组；不选则记为未分组。本批新写入或被覆盖的账号，都会使用这次选择。

### 重复邮箱如何处理

系统允许同一个邮箱保留多条记录，用导入时间区分不同版本。这样做是为了同一邮箱更换令牌后，仍能对照历史凭证。

- 本批邮箱在系统中都不存在：直接写入，并记录导入时间。
- 本批中有已存在的邮箱：先提示重复名单，再统一选择处理方式。
  - **追加新版本**：为每个重复邮箱再增加一条记录，旧版本保留。
  - **覆盖最新**：只更新该邮箱当前最新的那一条凭证，并刷新其导入时间；更早的历史版本不动。覆盖时，该条记录会改到本次所选分组（也可改为未分组）。
- 本批里系统中尚不存在的邮箱：无论选择追加还是覆盖，都按新账号写入本次所选分组（或不分组）。

### 清空后导入

这是单独的危险操作，与「覆盖最新」不是一回事。确认后会删除系统中现有全部账号，再把本次文本按当前选择写入（分组或未分组）。已有的分组本身不会被删掉。

### 删除账号

删除针对某一条账号版本，不影响同一邮箱的其他版本。删除后不可恢复。

### 批量操作

列表可勾选多条账号（含表头全选）。勾选后可以：

- **批量删除**：一次性删除所选账号版本，需确认，删除后不可恢复。
- **移至分组**：把所选账号改到某个分组，也可以改为未分组。

### 令牌状态

列表会显示该条凭证当前是否可用，便于判断能否读信：

- **未知**：刚导入，尚未实际读信校验。
- **正常**：最近一次换取访问权限成功。
- **异常**：刷新令牌换取失败。鼠标悬停可查看原因。常见原因包括令牌失效、租户不一致，需要重新授权后导入。

---

## 分组管理

账号可以归入某个分组，也可以不分组。不分组时列表显示为「未分组」。系统不会自动创建任何分组。

可以新建分组、重命名分组。导入账号时可选已有分组，也可以保持未分组。

删除分组后，该组内账号变为未分组。

「清空后导入」只清空账号，不会清空分组。

---

## 查看邮件

在账号列表中点「查看」，进入该条凭证对应的收件箱。读信使用这一条记录上的 Client ID 和刷新令牌，因此同一邮箱的不同版本可以分别尝试。

收件箱按时间倒序列出邮件摘要（主题、发件人、时间）。点开一封后可查看正文。网页格式的正文在隔离环境中展示，纯文本则直接阅读。

若令牌异常，将无法拉取邮件，需回到账号管理更换凭证。

---

## 本版不做的事项

当前版本不提供导出备份、批量复制，也不支持用文件上传代替文本粘贴导入。
